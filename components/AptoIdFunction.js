

import {ApolloClient,InMemoryCache,gql, ApolloProvider, useQuery, useMutation } from '@apollo/client'

export const AptoIdFunction = ({AptopNum,edificioId}) => {
    
    
    
    
    
    if (data!==null){

        return data.getApartamentoByEdifIdByAptoNum.id
    }else{
        return null
    }
    
    
    
}

export default AptoIdFunction;